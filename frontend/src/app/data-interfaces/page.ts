export interface Page {
    id: number;         // page number, also the id of the page
    content: string;    // url to page image
    artist: string;     // artist's name
    date: Date;
    round: number;
}